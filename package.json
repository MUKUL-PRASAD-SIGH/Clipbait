{
  "name": "epitychia",
  "version": "1.0.0",
  "description": "Smart clipboard application with AI-powered suggestions",
  "private": true,
  "workspaces": [
    "backend",
    "desktop",
    "mobile",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:desktop\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:desktop": "cd desktop && npm run tauri dev",
    "dev:mobile": "cd mobile && npm start",
    "build": "npm run build:backend && npm run build:desktop && npm run build:mobile",
    "build:backend": "cd backend && npm run build",
    "build:desktop": "cd desktop && npm run tauri build",
    "build:mobile": "cd mobile && npm run build",
    "test": "npm run test:backend && npm run test:desktop && npm run test:mobile",
    "test:backend": "cd backend && npm test",
    "test:desktop": "cd desktop && npm test",
    "test:mobile": "cd mobile && npm test",
    "lint": "npm run lint:backend && npm run lint:desktop && npm run lint:mobile",
    "lint:backend": "cd backend && npm run lint",
    "lint:desktop": "cd desktop && npm run lint",
    "lint:mobile": "cd mobile && npm run lint",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "setup": "npm install && npm run setup:backend && npm run setup:desktop && npm run setup:mobile",
    "setup:backend": "cd backend && npm install",
    "setup:desktop": "cd desktop && npm install",
    "setup:mobile": "cd mobile && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}